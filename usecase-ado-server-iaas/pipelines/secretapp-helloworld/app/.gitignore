# Build artifact — the sealed secret bundle is generated by
# helper-scripts/Build-SecretAppSealed.ps1 and baked into the image at build
# time. It must never be committed (it is regenerated per build and its hash
# feeds the CCE policy / SKR release policy binding).
sealed-data.enc
